- name: Set up Kubernetes cluster
  hosts: all
  become: true
  tasks:
    - name: Disable swab
      ansible.builtin.command: swapoff -a
      changed_when: true
    - name: Remove swap entry from /etc/fstab
      ansible.builtin.replace:
        path: /etc/fstab
        regexp: '(^.*swap.*$)'
        replace: '#\1'
    - name: Install required packages
      ansible.builtin.apt:
        name:
          - apt-transport-https
          - ca-certificates
          - curl
          - gnupg
          - lsb-release
          - socat
          - net-tools
        state: present
    - name: Add Kubernetes GPG key
      ansible.builtin.apt_key:
        url: https://pkgs.k8s.io/core:/stable:/v1.29/deb/Release.key
        state: present
        keyring: /etc/apt/keyrings/kubernetes-apt-keyring.gpg
    - name: Add Kubernetes repository
      ansible.builtin.apt_repository:
        repo: deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.gpg] https://pkgs.k8s.io/core:/stable:/v1.29/deb/ /
        state: present
    - name: Install Kubernetes components
      ansible.builtin.apt:
        name:
          - kubelet
          - kubeadm
          - kubectl
          - kubernetes-cni
          - containerd
        state: present
        update_cache: true
    - name: Create containerd configuration directory
      ansible.builtin.file:
        path: /etc/containerd
        state: directory
        mode: '0755'
    - name: Generate default containerd configuration
      ansible.builtin.shell: |
        rm -f /etc/containerd/config.toml
        containerd config default > /etc/containerd/config.toml
      changed_when: true
    - name: Configure containerd to use systemd as cgroup driver
      ansible.builtin.replace:
        path: /etc/containerd/config.toml
        regexp: 'SystemdCgroup = false'
        replace: 'SystemdCgroup = true'
    - name: Restart containerd service
      ansible.builtin.service:
        name: containerd
        state: restarted
        enabled: true
    - name: Configure network settings
      block:
        - name: Enable IP forwarding
          ansible.posix.sysctl:
            name: net.ipv4.ip_forward
            value: '1'
            state: present
            reload: true
        - name: Load bridge module
          community.general.modprobe:
            name: bridge
            state: present
        - name: Load br_netfilter module
          community.general.modprobe:
            name: br_netfilter
            state: present
        - name: Ensure net.bridge.bridge-nf-call-iptables is set in sysctl.conf
          ansible.builtin.lineinfile:
            path: /etc/sysctl.conf
            regexp: '^net.bridge.bridge-nf-call-iptables'
            line: 'net.bridge.bridge-nf-call-iptables = 1'
            state: present
        - name: Apply sysctl settings from /etc/sysctl.conf
          ansible.builtin.command: sysctl -p /etc/sysctl.conf
          changed_when: true
        - name: Set node IP address
          ansible.builtin.replace:
            path: /etc/default/kubelet
            regexp: 'KUBELET_EXTRA_ARGS='
            replace: 'KUBELET_EXTRA_ARGS=--node-ip={{ ansible_host }}'
- name: Initialize Kubernetes master node
  hosts: masters
  become: true
  tasks:
    - name: Initialize Kubernetes master node
      ansible.builtin.command: kubeadm init --pod-network-cidr=10.244.0.0/16 --apiserver-advertise-address=192.168.56.10
      changed_when: true
    - name: Create .kube directory
      ansible.builtin.file:
        path: /root/.kube
        state: directory
        mode: '0755'
    - name: Copy Kubernetes configuration file
      ansible.builtin.command: "{{ item }}"
      with_items:
        - cp -i /etc/kubernetes/admin.conf ~/.kube/config
      changed_when: true
    - name: Export KUBECONFIG environment variable
      ansible.builtin.command: echo "export KUBECONFIG=/root/.kube/config" >> /root/.bashrc
      changed_when: true
    - name: Install flannel CNI plugin
      ansible.builtin.command: kubectl apply -f https://github.com/flannel-io/flannel/releases/latest/download/kube-flannel.yml
      changed_when: true
    - name: Set up change namespace alias
      ansible.builtin.command: echo "alias kns='kubectl config set-context --current --namespace " >> /root/.bashrc
      changed_when: true
    - name: Install helm if not exists
      ansible.builtin.unarchive:
        src: https://get.helm.sh/helm-v3.11.0-linux-amd64.tar.gz
        dest: /usr/local/bin
        extra_opts: "--strip-components=1"
        owner: root
        group: root
        mode: '0755'
        remote_src: true
      args:
        creates: /usr/local/bin/helm
- name: Join worker nodes to the Kubernetes cluster
  hosts: workers
  become: true
  tasks:
    - name: Fetch join command from master
      ansible.builtin.command: kubeadm token create --print-join-command
      delegate_to: master
      register: join_command
      changed_when: false
    - name: Join worker nodes to the Kubernetes cluster
      ansible.builtin.command: "{{ join_command.stdout_lines[0] }} " 
      changed_when: true
...